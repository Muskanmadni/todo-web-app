# Default values for todo-app

# Configuration for the todo-backend subchart
todo-backend:
  replicaCount: 1
  image:
    repository: todo-backend
    pullPolicy: IfNotPresent
    tag: "latest"
  service:
    type: ClusterIP
    port: 8000
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 200m
      memory: 256Mi
  dapr:
    enabled: true
    appId: "todo-backend"
    appPort: 8000
    config: "dapr-config"

# Configuration for the todo-frontend subchart
todo-frontend:
  replicaCount: 1
  image:
    repository: todo-frontend
    pullPolicy: IfNotPresent
    tag: "latest"
  service:
    type: LoadBalancer
    port: 3000
  ingress:
    enabled: false
    className: ""
    annotations: {}
    hosts:
      - host: chart-example.local
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls: []
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi

# Dapr configuration
dapr:
  enabled: true
  components:
    pubsub:
      type: pubsub.in-memory
      version: v1
      metadata: []
    statestore:
      type: state.in-memory
      version: v1
      metadata: []
    secrets:
      type: secretstores.kubernetes
      version: v1
      metadata: []

# Kafka configuration
kafka:
  enabled: false
  replicaCount: 1
  auth:
    enabled: false
  persistence:
    enabled: false